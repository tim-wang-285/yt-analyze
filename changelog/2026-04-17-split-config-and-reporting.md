# 2026-04-17: Split config and reporting helpers

- extracted configuration constants and regexes into `config.py`
- moved topic timeline/yearly aggregation and report writers into `reporting.py`
- consolidated shared helper utilities into `helpers.py` and updated imports
